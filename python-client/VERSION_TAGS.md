# 版本 Tag 说明

每个 tag 对应一次可上传的 `lychee-python-client.zip`，并记录**线上对战**、**原因分析**与**已知缺陷**。  
查看 tag 摘要：`git show v4.2-contest-guard`  
完整分析：本文件各章节的「原因分析」  
打新 tag：`python tag_release.py v4.x-name`

---

## v4.2-contest-guard（`3db7e66`）— **当前推荐**

### 策略变更

- 窗口争夺输掉后 `WAITING` 不再死等，继续走路线的 `MOVE`
- 在边上遇敌方 guard 时主动 `BREAK_GUARD` / `FORCED_PASS`，不再 passive wait 等 guard 衰减

### 线上对战

| 对手 | 我方 | 对手 | 送达 | 说明 |
|------|------|------|------|------|
| — | — | — | — | **v4.2 尚未上线实测**；上传后请补 replay 到 `log/` |

### 原因分析（基于 v4.1 线上失败归纳）

| 失败模式 | 根因（代码/策略） | v4.2 修复 |
|----------|-------------------|-----------|
| S07 争夺后整局停滞 | `_main_action` 在 `WAITING` 且无 `nextNodeId` 时直接 `return WAIT`，不再执行后续 MOVE/破守逻辑 | 去掉该死循环分支，争夺结束后继续选路 |
| S09→S10 等 guard 自然衰减 | `_edge_guard_response` 遇 guard 只 `WAIT`，不调用 `_guard_breakthrough_action`；破守逻辑仅在 `IDLE` 节点触发 | 边上 blocked 时同样尝试 `BREAK_GUARD` |
| 分数卡在 80～90 | 上述两个问题导致 **delivery=0**，得分几乎只剩任务分 | 修复后应能推进至 S14 验关 + S15 送达 |

### 针对 v4.1 已修复的缺陷

| 缺陷 | 表现 | replay 证据 |
|------|------|-------------|
| S07 争夺后卡死 | 输掉 T_010 窗口争夺后 R191–R600 无限 `WAIT`，未送达 | `log/2614/replay (3).txt` |
| S09 边上等 guard | R330–R492 在 S09→S10 等 def6 衰减，无 `BREAK_GUARD`，来不及送达 | `log/2616/replay (2).txt` |

### 待验证（上线后看 replay）

- [ ] 输掉窗口争夺后能否继续前往 S09/S14
- [ ] S09→S10 是否出现 `BREAK_GUARD`
- [ ] 是否有 `DELIVER` / `delivered=true`

---

## v4.1-pack-checks（`9d6c7ab`）

### 策略变更

- 策略同 `v4-guard-break`，无新逻辑
- 新增 `pack_checks.py`、`UPLOAD_CHECKLIST.md`，打包前自动跑测试与 ZIP 校验

### 线上对战

| 对手 | 我方 | 对手 | 送达 | replay |
|------|------|------|------|--------|
| 2614 | 80 | 748 | 否 | `log/2614/replay (3).txt` |
| 2616 | 90 | 719 | 否 | `log/2616/replay (2).txt` |

### 原因分析

#### vs 2614（80 : 748）— `log/2614/replay (3).txt`

| 项目 | 我方 | 对手 | 差距 |
|------|------|------|------|
| 总分 | 80 | 748 | -668 |
| 送达 | 否 | 是 R499 | delivery 240 分全丢 |
| 任务分 | ~80（3 个任务） | ~170（4 个） | -90 |
| 鲜度 | 66.6（未送达） | 90.4 | — |
| 路线 | 官道 152 回合后停 S07 | 官道 419 回合全程 | — |

**时间线**

1. R43–61：S02 窗口争夺消耗若干回合（与 2616 类似），未致命。
2. R99/105/186：完成 3 个皇榜任务（缺第 4 个 → 任务分上限约 80）。
3. R187：2614 在 S07 对 T_010 发起窗口争夺；我方 3 轮 ABSTAIN，R190 输。
4. **R191–R600**：状态 `WAITING`、无 `nextNodeId`，客户端每回合发 `WAIT`（共 **410 次**），Freshness 89.6→66.6，从未离开 S07。

**根因**：输掉窗口争夺后进入 `WAITING`，旧逻辑认为「在边上才需要 MOVE」——此时已无 `nextNodeId`，于是一直 `WAIT`，**整局后 409 回合零进展**。与 guard 无关，纯状态机 bug。

#### vs 2616（90 : 719）— `log/2616/replay (2).txt`

| 项目 | 我方 | 对手 | 差距 |
|------|------|------|------|
| 总分 | 90 | 719 | -629 |
| 送达 | 否 | 是 R476 | delivery 240 分全丢 |
| 任务分 | ~80（4 任务完成但计分仍低） | ~170 | — |
| 鲜度 | 80.9（未送达） | 72.6（已送达仍赢） | — |
| 路线 | 官道 355 回合，到 S11 未走完 | 水路+官道混合 | — |

**时间线**

1. R99/105/188/192：4 个任务均完成，前期节奏正常。
2. R298：2616 在 **S10 设 guard（def=6，RED）**；我方已在 S09→S10 边上。
3. **R330–R492**：在 S09 对 S10 guard **passive wait 约 160 回合**，guard 从 6 自然衰减；整场 **无 `BREAK_GUARD` / `FORCED_PASS`**（有 99 好果足够 2 好+1 坏破 def6）。
4. R517：guard 弱至 def=1 才进入 S10；R518 bounty 打掉 S11 guard。
5. **R600**：仍在 S11→S12 移动，**游戏结束未验关、未送达**。

**根因**：`_edge_guard_response` 遇 guard 只返回 `WAIT`；`BREAK_GUARD` 仅在节点 `IDLE` 时由 `_guard_breakthrough_action` 触发——**人在边上永远进不了破守分支**。被动等 guard 衰减浪费 ~160 回合，后半程来不及 RUSH 验关送货。

### 已知缺陷（汇总）

| 缺陷 | 表现 | 根因 |
|------|------|------|
| S07 争夺后卡死 | 410× `WAIT` @ S07 | `WAITING` + 无 `nextNodeId` → 死循环 |
| S09 边上不破守 | 160 回合空等 guard | 边上只 WAIT，不 BREAK_GUARD |
| 无 BREAK_GUARD | 整场无破守动作 | 破守逻辑未挂到 `MOVING`/`WAITING` 分支 |

---

## v4-guard-break（`421dbf2`）

### 策略变更

- 官道路线 S02→S03→S07→…→S15，冰鉴/战马/RUSH_PROTECT
- 新增 `BREAK_GUARD` / `FORCED_PASS`、验关后送货、防回退

### 线上对战

| 对手 | 我方 | 对手 | 送达 | replay |
|------|------|------|------|--------|
| 2616 | 80 | 719 | 否 | `log/2616/replay (1).txt` |

本地自测：约 **761** vs Demo ~458，可送达 R482（无对手 guard/窗口压力）。

### 原因分析 — vs 2616（80 : 719）— `log/2616/replay (1).txt`

| 项目 | 我方 | 对手 |
|------|------|------|
| 总分 | 80 | 719 |
| 送达 | 否 | 是 |
| 任务 | 少量 | 4 任务 ~170 分 |

**时间线**

1. R298 / R365：2616 在 **S10、S11** 各设 guard（def=6）。
2. 我方在 S09→S10 反复 `MOVE`，遭 **`MOVE_BLOCKED_BY_GUARD` 共 194 次**。
3. 虽已有 `_guard_breakthrough_action`，但仅在 **节点 IDLE** 时调用；在边上只走 `_edge_guard_response` → **WAIT 或重复 MOVE**，从未发出 `BREAK_GUARD`。
4. 最终停在 **S08 附近**，Freshness 耗尽，delivery=0 → 总分 80。

**根因**：v4 实现了破守函数，但**调用点缺失**——线上有 guard 的对手（2616）一设防就卡住；本地 Demo 无 guard，故自测 761 分看不出问题。

**与 v4.1 的关系**：v4.1 策略代码相同，2616 第二场（replay 2）从「硬撞 blocked」变成「被动 wait」，分数 80→90、推进到 S11，但仍未破守/未送达——**同一根因的不同表现**。

### 已知缺陷（v4.2 前）

| 缺陷 | 表现 | 根因 |
|------|------|------|
| 边上遇 guard | 194× blocked 或长时间 WAIT | 破守未接入边态分支 |
| 窗口争夺后卡死 | S07 无限 WAIT（v4.1 才暴露） | 同 v4.1 |
| 本地/线上差距大 | 自测 761，线上 80 | Demo 无 guard + 无窗口争夺压力 |

---

## 预 v4 — 水路旧客户端（无 tag）

### 线上对战

| 对手 | 我方 | 对手 | 送达 | replay |
|------|------|------|------|--------|
| 2614 | 684 | 761 | 是 R466 | `log/2614/replay.txt` |

### 原因分析 — vs 2614（684 : 761）

| 项目 | 我方 | 对手 | 差距 |
|------|------|------|------|
| 总分 | 684 | 761 | -77 |
| 送达 | 是 R466 | 是 R490 | 鲜度 74.1 vs 90.9 |
| 任务分 | ~125（3 任务） | ~170（4 任务） | **-45** |
| 路线 | 水路 114 回合 | 官道为主 419 回合 | 鲜度损耗大 |

**时间线**

1. 走 **水路**（S02→S04→…），114 回合水上移动，鲜度掉得比官道快。
2. 只完成 **3 个皇榜任务**（缺 S09/S10/S11/S13 路线上的任务规划）。
3. R466 送达，但鲜度 **74.1** vs 对手 **90.9**（-30 分档）；任务 -45 → 总分落后。

**根因**：策略偏水路抄近/绕资源，未走官道任务链；**不是卡死 bug**，是路线与任务规划落后。v4 官道+4 任务设计即针对此问题。

---

## 打 tag 规范

```powershell
cd python-client
# 1. 更新本文件（线上对战 + 原因分析）
python pack_submission.py
python tag_release.py v4.3-xxx --recommend --force
git push origin master
git push --force origin v4.3-xxx
```

`tag_release.py` 从本 tag 章节生成 annotated 消息（含对战表与原因分析）。**上线后请先更新「原因分析」再重打 tag。**
